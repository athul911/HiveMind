apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hivemind.fullname" . }}-config
  labels:
    {{- include "hivemind.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
